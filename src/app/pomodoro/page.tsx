"use client";

import Pomodoro from "@/components/pomodoro";

const PomodoroPage = () => {
  return (
    <div className="h-screen w-full">
      <Pomodoro />
    </div>
  );
};

export default PomodoroPage;
